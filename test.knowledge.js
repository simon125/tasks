// write some closure

// write how you can handle async actions in js - code 2 ways

// write function to check if array elements are only numerical then return true

// write 4 basic calc function in pure functional style and object oriented style

// *write "chainable" class - person.walk().sit().eat().repeat()

// *write requrent function - add(3)(3)(3)() == 9 && add(2)(2)() == 4
